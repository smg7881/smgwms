<div data-controller="std-region-grid"
     data-action="ag-grid:ready->std-region-grid#registerGrid"
     data-std-region-grid-batch-url-value="<%= batch_save_url %>">
  <%= render Ui::SearchFormComponent.new(
    url: create_url,
    fields: search_fields,
    cols: 4,
    enable_collapse: true
  ) %>

  <%= render Ui::GridHeaderComponent.new(
    icon: "map", title: "권역 목록", grid_id: "std-region",
    buttons: [
      { label: "행추가", class: "btn btn-sm btn-primary", action: "click->std-region-grid#addRow" },
      { label: "행삭제", class: "btn btn-sm btn-danger", action: "click->std-region-grid#deleteRows" },
      { label: "저장", class: "btn btn-sm btn-success", action: "click->std-region-grid#saveRows" }
    ]
  ) %>

  <%= render Ui::AgGridComponent.new(
    columns: columns,
    url: grid_url,
    height: "calc(100vh - 370px)",
    page_size: 20,
    row_selection: "multiple",
    grid_id: "std-region",
    data: { std_region_grid_target: "grid" }
  ) %>
</div>
