<div data-controller="std-business-certificate-grid"
     data-action="ag-grid:ready->std-business-certificate-grid#registerGrid"
     data-std-business-certificate-grid-batch-url-value="<%= batch_save_url %>">
  <%= render Ui::SearchFormComponent.new(
    url: create_url,
    fields: search_fields,
    cols: 3,
    enable_collapse: true
  ) %>

  <%= render Ui::GridHeaderComponent.new(
    icon: "id-card", title: "사업자등록증 목록", grid_id: "std-business-certificate",
    buttons: [
      { label: "추가", class: "btn btn-sm btn-primary", action: "click->std-business-certificate-grid#addRow" },
      { label: "삭제", class: "btn btn-sm btn-danger", action: "click->std-business-certificate-grid#deleteRows" },
      { label: "저장", class: "btn btn-sm btn-success", action: "click->std-business-certificate-grid#saveRows" }
    ]
  ) %>

  <%= render Ui::AgGridComponent.new(
    columns: columns,
    url: grid_url,
    height: "calc(100vh - 360px)",
    page_size: 20,
    row_selection: "multiple",
    grid_id: "std-business-certificate",
    data: { std_business_certificate_grid_target: "grid" }
  ) %>
</div>
