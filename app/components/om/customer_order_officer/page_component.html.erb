<div data-controller="om-customer-order-officer-grid"
     data-action="ag-grid:ready->om-customer-order-officer-grid#registerGrid"
     data-om-customer-order-officer-grid-batch-url-value="<%= batch_save_url %>">
  <%= render Ui::SearchFormComponent.new(
    url: create_url,
    fields: search_fields,
    cols: 4,
    enable_collapse: true
  ) %>

  <div class="flex items-center justify-between gap-3 mb-3 mt-2">
    <h3 class="inline-flex items-center gap-2 text-[15px] font-semibold text-text-primary">
      <%= helpers.lucide_icon("users", css_class: "w-4 h-4 text-text-secondary") %>
      고객오더담당자 목록
      <%= render Ui::GridActionsComponent.new(grid_id: "om-customer-order-officer") %>
    </h3>
    <%= render Ui::GridToolbarComponent.new(
      buttons: [
        { label: "추가", class: "btn btn-sm btn-primary", action: "click->om-customer-order-officer-grid#addRow" },
        { label: "삭제", class: "btn btn-sm btn-danger", action: "click->om-customer-order-officer-grid#deleteRows" },
        { label: "저장", class: "btn btn-sm btn-success", action: "click->om-customer-order-officer-grid#saveRows" }
      ]
    ) %>
  </div>

  <%= render Ui::AgGridComponent.new(
    columns: columns,
    url: grid_url,
    height: "calc(100vh - 380px)",
    page_size: 20,
    row_selection: "multiple",
    grid_id: "om-customer-order-officer",
    data: { om_customer_order_officer_grid_target: "grid" }
  ) %>
</div>
